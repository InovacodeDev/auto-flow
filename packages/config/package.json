{
    "name": "@autoflow/config",
    "version": "1.0.0",
    "description": "Shared configuration for AutoFlow",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rm -rf dist",
        "type-check": "tsc --noEmit"
    },
    "devDependencies": {
        "typescript": "^5.2.2",
        "@types/node": "^20.5.7"
    }
}